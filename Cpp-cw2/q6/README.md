# Some Explanations

The main1.cpp works on its own. It is exactly what the question asked for.

The main2.cpp needs to be compiled with vector.cpp. The size of the vector int nn is changed to std::size_t nn. The seperate files + unsigned vector sizes is a cherry on top.